import MainScreen from './MainScreen';

export default {
  module: MainScreen,
  name: 'MainScreen',
  options: ({ navigation }) => ({
    headerLeft: null,
    headerTitle: null,
    headerRight: null,
  }),
};
