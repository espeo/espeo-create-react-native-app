import ProfileScreen from './ProfileScreen';

export default {
  module: ProfileScreen,
  name: 'ProfileScreen',
  options: ({ navigation }) => ({
    headerLeft: null,
    headerTitle: null,
    headerRight: null,
  }),
};
