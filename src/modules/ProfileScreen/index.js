import Profile from './Profile';

export default {
  module: Profile,
  name: 'Profile',
  options: ({ navigation }) => ({
    headerLeft: null,
    headerTitle: null,
    headerRight: null,
  }),
};
