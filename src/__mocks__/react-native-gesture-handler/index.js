const GestureHandler = {
  Directions: {},
};

module.export = GestureHandler;

import { TouchableWithoutFeedback } from 'react-native';

export const BaseButton = TouchableWithoutFeedback;
export const BorderlessButton = TouchableWithoutFeedback;
